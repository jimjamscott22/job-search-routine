{
  "name": "job-search-routine",
  "version": "1.0.0",
  "private": true,
  "description": "Local daily job-search routine checklist with on-page reports",
  "scripts": {
    "test": "node --test tests/*.test.js"
  }
}
